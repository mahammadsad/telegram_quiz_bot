{
  "name": "telegram-quiz-platform-browser-tests",
  "version": "8.7.1",
  "private": true,
  "description": "Deterministic mobile browser tests for the Telegram quiz platform",
  "scripts": {
    "test:browser": "playwright test",
    "test:browser:service-worker": "playwright test --config=playwright.service-worker.config.js",
    "test:browser:report": "playwright show-report"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.62.1"
  }
}
