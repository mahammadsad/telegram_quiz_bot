# Audit remediation status

Status as of 20 August 2026 for audit commit `cf51b4ebb9d4a3619968d39f710a616a91284181`.

Status meanings:

- **Implemented**: code and local automated evidence are complete; production impact still requires the release procedure.
- **Partial**: a material part is complete, but the full audit acceptance condition is not.
- **Awaiting external action**: credentials, production data, platform ownership, legal ownership, or a paid/reliable control plane is required.
- **Open**: not safely completed in this remediation branch.
- **No longer applicable**: current repository evidence disproves the finding or the condition was already fixed.

| ID | Severity | Current evidence | Status | Files / verification | Residual risk and external action |
|---|---|---|---|---|---|
| P0-01 | P0 | The canonical Render host is deployed at `https://telegram-quiz-bot-h7p1.onrender.com`; `/health/live`, `/health/ready`, `/version`, the Bengali hub, and the answer-free recent catalogue returned HTTP 200 after commit `0b625cfa`. Blueprint, smoke workflow, and rollback guide now reference that host. | **Partial** | `render.yaml`, `config/settings.py`, `miniapp-shell.js`, `manifest.webmanifest`, `service-worker.js`, `app.py`, `scripts/deployed_smoke.py`, `.github/workflows/deployed-smoke.yml`; production endpoint evidence and route/deployment contract tests. | Run the authenticated deployed smoke with a designated synthetic Telegram account and rehearse rollback. GitHub Pages remains a preview, not the authenticated application. |
| P0-02 | P0 | Expiry ordering is repaired and each dispatcher heartbeat evaluates all 13 due-subject states, including unclaimed dead letters and overdue/missing jobs. The production dispatcher run on 2026-08-23 posted Miscellaneous and Reasoning, while it fail-closed Computer on a near-duplicate collision, Mathematics on indistinguishable options, and English on independent-verifier rejection. | **Partial / awaiting external action** | `scripts/refresh_current_affairs_sources.py`, `services/quiz_dispatcher.py`, `bot.py`; expiry/global-state tests and production dispatcher evidence. | A reviewed 7–14 day reserve inventory is required for Computer, Mathematics and English; do not replay dead letters or force-post rejected content. A reliable primary scheduler remains external/product work. |
| P0-03 | P0 | Daily attempts start on the server and ranking duration is database-derived; the required migration is applied in production and readiness is green. Client duration/response times remain untrusted telemetry. | **Implemented / deployed** | `20260820090000_server_timed_daily_attempts.sql`, `app.py`, `index.html`, attempt repository/service tests and production readiness evidence. | Legacy clients remain compatible but have null/untrusted timing and cannot receive a trusted time tie-break. Monitor anomaly reason codes after a representative attempt sample. |
| P1-01 | P1 | Generator and verifier identities are stored separately; same provider/model cannot be labeled independent; a different configured model can be selected. The production migration is applied and readiness is green. | **Implemented / deployed** | `services/gemini_provider_pool.py`, `services/question_verification.py`, verification audit repository, `20260820100000_question_verification_independence.sql`; independence tests and production contract evidence pass. | Historical records need no destructive rewrite but retain their historical basis. Monitor failure rates across normal scheduled production runs. |
| P1-02 | P1 | Publication fails when a source-less question is checked by the same model without another evidence basis. | **Partial** | `services/question_verification.py`; proof-required and same-model tests pass. | Subject-specific deterministic solvers and mandatory approved citations for every applicable daily path are not complete. Keep unproved inventory out of production approval. |
| P1-03 | P1 | PIB ingestion is hardened and the official RBI press-release RSS adapter now uses the same host, redirect, XML, date, provenance, exact-span and freshness controls. Current-affairs chapter/category breadth remains limited. | **Partial** | `scripts/refresh_current_affairs_sources.py`; PIB/RBI parser, expiry, provenance and event tests pass. | Add further approved official adapters and reviewed category/chapter coverage before claiming complete breadth; source-rights review remains mandatory for every new source family. |
| P1-04 | P1 | Heartbeats query global due state and fail on missing, overdue, blocked, retry-exhausted, unknown or dead-lettered jobs. The 2026-08-23 production dispatcher surfaced actionable Computer/Mathematics/English failures while safely posting Miscellaneous and Reasoning. | **Partial / deployed** | `services/quiz_dispatcher.py`, `bot.py`; dead-letter/overdue tests, production dispatcher evidence and repaired-main preflight pass. | Production alert routing and a complete 13/13 normal day must still be observed. Static fallback export after a successful valid pack remains in the existing execution path and should be smoke-tested live. |
| P1-05 | P1 | Render remains on the free plan and GitHub Actions remains the scheduler. | **Awaiting external action** | `render.yaml`, `.github/workflows/main.yml`. | Platform owner must select and fund an always-on worker/database/edge cron, move primary dispatch to it, keep Actions as recovery, and verify SLA/alerts. No paid plan was enabled automatically. |
| P1-06 | P1 | Unconditional AdSense loading is removed; the Bengali hub, mock, practice, dashboard and settings pages load versioned external CSS/JS, CSP blocks inline scripts and styles, and dynamic progress uses semantic controls/SVG attributes rather than inline styles. | **Partial** | `miniapp-shell.js`, `index.css`/`index.js`, `mock.css`/`mock.js`, `practice.css`/`practice.js`, `dashboard.css`/`dashboard.js`, `settings.css`/`settings.js`, `legal.css`, `app.py`; route, CSP and mobile-browser checks pass. | Keep third-party ads off authenticated/timed pages. Reassess any future inline style requirement before weakening CSP. |
| P1-07 | P1 | Draft privacy/terms pages, authenticated export, deletion request/cancel, grace period and pseudonymous audit records are implemented. | **Partial / awaiting external action** | `privacy.html`, `terms.html`, privacy service/repository, `20260820120000_privacy_rights.sql`, `settings.html`; privacy tests pass. | Legal/controller/contact placeholders require qualified review. Do not enable deletion processing until backups, retention scope and restore/rollback are approved and tested. Consent history beyond existing preferences remains incomplete. |
| P1-08 | P1 | Vulnerable locks are updated (`cryptography 50.0.0`, `h2 4.4.1`); FastAPI, GenAI, database/client tooling and browser-test locks are refreshed; RSS uses `defusedxml` with size/type/redirect/host/time limits; security automation includes direct-requirement/lockfile parity checks. | **Implemented** | requirements/locks, `scripts/check_lockfile_parity.py`, refresh script, Dependabot and security workflow; Ruff, pytest, mypy, high-severity Bandit and npm audit pass locally where noted below. | Repository owner must enable GitHub secret scanning/push protection and review Dependabot/CodeQL results. Local `pip-audit` can be network-bound; GitHub Security is the release gate. |
| P1-09 | P1 | `mock.html` without an ID now displays an answer-free searchable/filterable catalog with type, exam, subject, duration, marking and availability. | **Partial** | catalog migration/repository/service/API, `mock.html`; catalog and route tests pass. | Bookmarks, complete year/shift hierarchy and authenticated attempt-state enrichment remain open. Apply the catalog migration before app release. |
| P1-10 | P1 | Saved preferences still do not drive assignments. | **Open** | Existing settings and learning services. | Product/learning-science owner must define assignment policy and tests. Until then, copy must not claim that broadcast quizzes are personalized. |
| P1-11 | P1 | Global outcome reason codes and safer release/readiness evidence improve operator diagnostics. | **Partial** | dispatcher, readiness and version endpoint. | No role-based admin console, durable SLO dashboard, tracing/error aggregation or complete alert routing exists. |
| P1-12 | P1 | Immutable answer-free quiz/test payloads now have ETags and public CDN caching; personalized responses remain `no-store`; write rate limits remain. | **Partial** | `app.py`; API caching/privacy tests pass. | Edge/IP read quotas, aggregate short caching, bot protection and query-budget monitoring require the production edge owner. |
| P1-13 | P1 | Pinned security/deployed-smoke workflows, Dependabot and immutable release metadata are present. | **Partial / awaiting external action** | `.github/workflows/security.yml`, `.github/workflows/deployed-smoke.yml`, `.github/dependabot.yml`, `/version`. | Repository admin must protect `main`, require CI/security/deployed-smoke checks, configure production environment approvals and decide how to move generated artifacts off `main`. |
| P2-01 | P2 | No reviewed corpus expansion was fabricated during remediation. | **Open** | Existing source inventory reports. | Content owner must approve blueprint-level inventory and provenance targets, then build reviewed reserves. |
| P2-02 | P2 | Existing collision and semantic-near checks remain; no full corpus cleanup/backfill was performed. | **Partial** | Existing validation/inventory services; regression suite passes. | Run a reviewed deduplication/backfill and enforce the audit target of <0.5% 30-day repeated exposure with zero same-test duplicate stems. |
| P2-03 | P2 | Authored difficulty remains; no statistically valid production sample was available. | **Open** | Existing attempt analytics only. | Data/learning-science owner must define minimum sample sizes, facility/discrimination/distractor metrics, retirement policy and mastery correction rules. |
| P2-04 | P2 | Learner reporting exists after daily and practice answers with reason/details, authenticated APIs and moderation thresholds. An additive, service-role-only reporter-status projection is prepared; it returns only a learner's own report/case status and closed-case resolution, never question text, options or answers. A manual, dry-run-first, production-gated workflow now detects an existing production migration-ledger mismatch before it can make any schema change. | **Partial / blocked on ledger reconciliation** | `20260823065257_learner_report_status_projection.sql`, `.github/workflows/supabase-migrations.yml`, `app.py`, moderation/report repositories and focused contract/API tests; dry run `32626519942`. | Production records 39 historical migration versions absent from this repository. Recover or independently verify their source/equivalence, then reconcile history in a reviewed maintenance change. Do **not** run `supabase migration repair` against production merely to bypass the gate. |
| P2-05 | P2 | Reminder/retention scheduling was not enabled and no real-user notifications were sent. | **Open** | Existing preference model. | Product owner must define consent, quiet hours, unsubscribe, scheduler and effectiveness metrics. |
| P2-06 | P2 | The product remains Bengali-only in the visible UI; translation catalogs were not introduced. | **Open** | Shared shell/settings. | Do not advertise Hindi/English until UI, content, search normalization and QA are end-to-end complete. |
| P2-07 | P2 | Anonymous web users remain preview-only. | **Open** | Existing Telegram auth boundary. | A guest/linking design requires identity, abuse, privacy and attempt-integrity review. Do not weaken Telegram HMAC validation. |
| P2-08 | P2 | Catalog discovery improves one journey step, but diagnostic, plan, syllabus map and adjustment loop remain incomplete. | **Partial** | `mock.html`, catalog API. | Product owner must sequence the complete exam-preparation journey and acceptance metrics. |
| P2-09 | P2 | Existing accessibility primitives are preserved, a manual WCAG 2.2 AA matrix is documented, and pinned axe-core Playwright checks cover the Bengali preparation hub, quiz start/result, authenticated dashboard, settings, revision-practice/feedback and mock-result views at all four Android target widths. The expanded checks corrected chart semantics and insufficient contrast. | **Partial** | `docs/ACCESSIBILITY_TEST_MATRIX.md`, `tests/browser/accessibility.spec.js`, `tests/browser/quiz-flow.spec.js`, `tests/browser/practice.spec.js`, `tests/browser/mock-flow.spec.js`, `dashboard.html`, `dashboard.css`, `practice.css`, `mock.css`; 32 automated axe checks pass. | Execute the manual matrix with assistive technologies before production sign-off; automation cannot verify real Telegram and assistive-technology behavior. |
| P2-10 | P2 | New privacy, timing, catalog and verification logic is placed in services/repositories; mock-test request contracts are isolated in `api_models.py`; primary pages use external CSS/JS and no longer need inline runtime styles. `app.py` and `bot.py` still contain large route/dispatch surfaces. | **Partial** | Service/repository modules, `api_models.py`, external frontend assets; targeted mock-test contract suite, Ruff and mypy pass. | Continue router and bot-dispatch extraction in behavior-preserving changes. |
| P2-11 | P2 | Accidental FUSE artifact is removed/ignored and current release, architecture and rollback documentation is added. | **Implemented** | `.gitignore`, removed `.fuse_hidden*`, remediation/release/architecture docs. | Archive contradictory legacy runbooks after owner review rather than deleting potentially useful history automatically. |
| P2-12 | P2 | Security, contribution, conduct, roadmap, issue/PR templates and content provenance policy are added. | **Partial / awaiting external action** | root governance files, `.github` templates, `docs/PUBLIC_ROADMAP.md`, `docs/CONTENT_PROVENANCE_AND_LICENSING.md`. | Repository owner must choose an OSI license and confirm code/content ownership. No license was guessed. |

## Verification evidence

- `ruff check .`: pass.
- `mypy`: success, 79 configured production source files.
- `pytest -q`: **481 passed, 33 skipped**, one upstream Starlette/httpx deprecation warning.
- `pip-audit -r requirements.lock`: no known vulnerabilities.
- `bandit -r . -lll`: no high-severity findings.
- `npm audit`: zero vulnerabilities at lock refresh.
- `git diff --check`: pass.
- Disposable PostgreSQL integration tests, Playwright browsers, live staging/production smoke and rollback rehearsal were unavailable locally and remain CI/release gates; they must not be inferred from unit tests.
